#!/usr/bin/env bash

_base="{{ config.ci.base }}"
_type="{{ config.ci.type }}"
_logs="{{ config.ci.logs }}"
_branch="{{ config.ci.branch }}"
_ci="{{ config.ci.destination }}"
_backend="{{ config.ci.backend }}"
_retries="{{ config.ci.retries }}"
_regexp="{{ config.backend.regexp }}"

export UPPER_CONSTRAINTS_FILE="$_base/requirements/upper-constraints.txt"
